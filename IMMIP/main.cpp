#include "glwidget.h"

int main(int argc, char* argv[])
{
    glwidget v;

    v.set_time_num(10);
    v.set_time_num(20);
    v.set_time_num(30);
    v.set_time_num(40);
    v.set_time_num(50);
    v.set_time_num(100);
    v.set_time_num(150);
    v.set_time_num(200);

    return 0;
}